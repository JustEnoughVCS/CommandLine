workspace_alias:
  error:
    undeclared_input: |
      不明确的输入！
      使用 `jvn helpdoc commands/workspace/alias` 查询使用方式

  render:
    map: |
      本地索引 `~%{local}` 被指向 `%{remote}`
    no_map: |
      本地索引 `~%{local}` 不存在指向

  comp:
    insert: 插入别名
    query: 查询别名
    erase: 删除别名
    to: 远程索引
