workspace_alias:
  error:
    undeclared_input: |
      Unclear input!
      Use `jvn helpdoc commands/workspace/alias` to look up the usage

  render:
    map: |
      Local index `~%{local}` is mapped to `%{remote}`
    no_map: |
      Local index `~%{local}` has no mapping

  comp:
    insert: Insert alias
    query: Query alias
    erase: Delete alias
    to: Remote index
